<div class="container signup edit">
  <% if @company.name.blank?%>
    <h2 class="signup__title">Please, fill the form</h2>
  <% else %>
    <h2 class="signup__title"><%= @company.name %> Page</h2>
  <% end %>

  <%= render 'form' %>

</div>